import React from 'react'
import { Button } from '@chakra-ui/react'
/**
* @author
* @function Seller
**/

export const Seller = (props) => {
  return(
    <Button variant={"ghost"}  onClick={props.onClick}>Become a Seller</Button>
   )
  }
